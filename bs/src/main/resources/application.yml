spring:
    application:
        name: broker-system

    artemis:
        mode: native
        broker-url: tcp://localhost:61616
        user: admin
        password: admin

    jackson:
        serialization:
            write-dates-as-timestamps: false

server:
    port: 8082

logging:
    level:
        com.instantpayments.bs: DEBUG
